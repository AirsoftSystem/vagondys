
/**
 * Layout vide pour la page de connexion admin
 * Aucun menu latéral, pleine page
 */
export default function AdminLoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
